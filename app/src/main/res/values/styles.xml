<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Spinner Style -->
    <style name="CustomSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@drawable/custom_spinner_background</item>
        <item name="android:textColor">@color/white</item> <!-- Cor do texto -->
        <item name="android:padding">8dp</item>
        <item name="android:popupBackground">@drawable/degrade_nebulosa</item> <!-- Fundo do popup -->
        <item name="android:spinnerMode">dropdown</item> <!-- Modo do Spinner -->
    </style>

    <!-- Text Style for Planet Info -->
    <style name="PlanetInfoText">
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">8dp</item>
    </style>

    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/black</item> <!-- Seletor de cores para ícones -->
        <item name="itemTextColor">@color/white</item> <!-- Seletor de cores para texto -->
        <item name="android:background">@color/popup_background_nebulosa</item> <!-- Cor de fundo -->
    </style>

    <style name="TransparentDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="TransparentActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">@android:color/transparent</item> <!-- Fundo transparente -->
        <item name="elevation">0dp</item> <!-- Remove sombra -->
    </style>

</resources>
